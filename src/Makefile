build:
	odin build . -out:../build/gbjam.exe -debug -show-timings -terse-errors -o:none

build-speed:
	odin build . -out:../build/gbjam.exe -show-timings -o:speed

build-aggressive:
	odin build . -out:../build/gbjam.exe -show-timings -o:aggressive

run:
	odin run . -out:../build/gbjam.exe -debug -show-timings -terse-errors -o:none

clean:
	rm ./build/Renderer
